# 第 3 阶段(Prisma 接入)所需,仅供 Prisma CLI 自动读取。
# 应用运行时本阶段尚未读取 env(等第 4 阶段接入 ConfigModule)。
# JWT_SECRET / APP_* / SUPER_ADMIN_* 留待第 4 / 9 阶段补充。
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app?schema=public
